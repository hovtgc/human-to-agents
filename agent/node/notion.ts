/** Notion holds the projects. It is not the wall.
 *
 * Dump the Projects database to a markdown `work` table and pass that
 * file as --mapping together with the channels table. No network.
 */
export function materialize(): never {
  throw new Error(
    "notion provider is a skeleton: dump Projects to markdown, then --mapping that file",
  );
}
